# tessyfarm_smartloop/backend_api/requirements.txt
fastapi>=0.110.0,<0.112.0
uvicorn[standard]>=0.29.0,<0.31.0
pydantic[email]>=2.5.0,<3.0.0 # For settings management and data validation
python-dotenv>=1.0.0,<2.0.0 # To load .env file for settings

# For PostgreSQL (will be used in the next step)
sqlalchemy>=2.0.0,<2.1.0
psycopg2-binary>=2.9.0,<2.10.0 # For connecting to PostgreSQL
alembic>=1.12.0,<1.14.0 # For database migrations

# Add other dependencies as needed
